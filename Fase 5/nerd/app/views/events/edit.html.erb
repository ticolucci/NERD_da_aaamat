<% content_for :title do %>
  <h1>Editando Evento</h1>
<% end %>

<% content_for :bottom_links do %>
  <%= link_to image_tag("/images/cancel.png", :title => "Cancelar")+"Cancelar", subject_event_path(@subject.id, @event.id) %>
<% end %>

<% form_for [@subject, @event] do |form| %>
  <%= render :partial => 'form', :locals => {:form => form, :submit_text => "Salvar"} %>
<% end %>