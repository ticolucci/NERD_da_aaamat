<% content_for :title do %>
  <h1>Editando Tarefa</h1>
<% end %>

<% content_for :bottom_links do %>
  <%= link_to image_tag("/images/cancel.png", :title => "Cancelar")+"Cancelar", subject_task_path(@subject.id, @task.id) %>
<% end %>

<% form_for [@subject, @task] do |form| %>
  <%= render :partial => 'form', :locals => {:form => form, :submit_text => "Salvar"} %>
<% end %>