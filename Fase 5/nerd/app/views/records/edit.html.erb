<% content_for :title do %>
  <h1>Editando Registro</h1>
<% end %>

<% content_for :bottom_links do %>
  <%= link_to image_tag("/images/cancel.png", :title => "Cancelar")+"Cancelar", subject_record_path(@subject.id, @record.id) %>
<% end %>

<% form_for [@subject, @record] do |form| %>
  <%= render :partial => 'form', :locals => {:form => form, :submit_text => "Salvar"} %>
<% end %>